version: "2.2"
services:
  postgres:
    image: postgres:14
    environment:
      - POSTGRES_USER=dboperator
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=postgres
    cpus: 32
    mem_limit: 1024m
    ports:
      - "5243:5432"
